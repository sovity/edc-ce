# Dependency versions have been moved to libs.version.toml
org.gradle.jvmargs=-Xmx4096m

# Build subprojects concurrently. The CE and EE trees are largely independent,
# so this mostly benefits the compile phase.
org.gradle.parallel=true
